// Compatibility entry only. The single source of truth lives in ../ProjectRoomPage.impl.
// Keep this file as a zero-logic re-export so any historical imports continue to work
// without carrying a second independent implementation.
export { default } from '../ProjectRoomPage.impl';
