# v1.0.7 迭代说明（2026-04-17）

## 背景
- 本次版本主要用于清理并同步本地长期堆积的有效改动，降低“本地漂移”和回归定位成本。
- 采用“先安全快照、再持续拆分优化”的策略，优先保证代码可追溯与可回滚。

## 本次同步范围
- 后端运行时与路由链路相关改动（`apps/api/src/agents`、`apps/api/src/routes`、`apps/api/src/data`、`apps/api/src/system`）。
- workflow-v2 / Stitch 协作链路相关改动（`apps/api/src/workflow-v2`）。
- 前端项目流转与 API 适配改动（`apps/web/src/App.tsx`、`apps/web/src/lib/api/core.ts`、`apps/web/src/pages/modals/hooks/useNewProjectModalController.ts`）。
- 共享类型与脚本能力更新（`packages/shared/src/index.ts`、`scripts/acceptance-real-3modes-v2.mjs`、`scripts/start-local-prod.sh`）。
- 新增清理脚本：`scripts/cleanup-legacy-template-projects.ts`。
- 验收与可见性报告同步：`docs/reports/*`。

## 验证结论
- `pnpm --filter @occ/api typecheck` 通过。
- `pnpm --filter @occ/web typecheck` 通过。
- `pnpm --filter @occ/api test:routes` 通过（19/19）。

## 说明
- `generated/` 目录为本地产物，未纳入本次版本提交。
- 后续会继续按功能域拆分更细粒度提交，避免再次形成大批量堆积。
