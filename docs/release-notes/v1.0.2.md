# v1.0.2 发布说明（2026-04-03）

## 本次修复
- 修复设计阶段“提交设计审查卡后被误判为可快速完结”的问题。
- 修复设计提交内容可被自动补齐为“看似完整稿”的问题，改为严格使用真实提交内容。
- 修复 reconcile 在当前进行中阶段自动补齐核心交付物的问题，避免模板稿冒充真实研发产物。
- 加强 DESIGN/DEV 模板门禁：拦截仅系统回填且缺少执行证据的交付物。

## 本次优化
- 新增并加强三轮生命周期深检脚本：
  - 单轮超时保护（默认 18 分钟，可配置）
  - 阶段心跳日志输出（每 8 轮）
  - 每轮增量落盘 `docs/reports/triple-lifecycle-deepcheck-latest.json`

## 验证
- `pnpm --filter @occ/api test:routes` 通过（17/17）
- `pnpm --filter @occ/api typecheck` 通过
- 三轮端到端项目检查通过（创建 -> ANALYSIS -> DESIGN -> DEV -> ACCEPT -> completed）
